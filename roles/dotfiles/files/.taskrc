# [Created by task 2.5.1 2/4/2017 21:12:23]
# Taskwarrior program configuration file.
# For more documentation, see http://taskwarrior.org or try 'man task', 'man task-color',
# 'man task-sync' or 'man taskrc'

# Here is an example of entries that use the default, override and blank values
#   variable=foo   -- By specifying a value, this overrides the default
#   variable=      -- By specifying no value, this means no default
#   #variable=foo  -- By commenting out the line, or deleting it, this uses the default

# Use the command 'task show' to see all defaults and overrides

# Files
data.location=~/.task

report.work.description=List of open tasks at work
report.work.columns=id,project,description.count
report.work.labels=ID,Proj,Desc
report.work.sort=project+/,entry+
report.work.filter=tag:work status:pending
