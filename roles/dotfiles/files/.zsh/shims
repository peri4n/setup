# vim: set ft=zsh:

# pyenv
eval "$(pyenv init -)"

# jenv
if which jenv > /dev/null; then eval "$(jenv init -)"; fi

# nvm
[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"

# rbenv
eval "$(rbenv init -)"

